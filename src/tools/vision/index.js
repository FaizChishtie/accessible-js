'use strict'

// export all public vision functions here

const colorblind = require('./colorblind');

const magnify = (block) => {
    return block;
}

module.exports = {
    magnify,
    colorblind,
}